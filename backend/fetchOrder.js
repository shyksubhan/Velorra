require('dotenv').config({ path: __dirname + '/.env' }); const { getDB, initFirebase } = require('./utils/firebase.js'); async function run() { initFirebase(); const db = getDB(); const snap = await db.collection('social_orders').doc('SOC-DC75E49D').get(); if (!snap.exists) { console.log('Order not found!'); return; } console.log(JSON.stringify(snap.data().items, null, 2)); } run().catch(console.error);
